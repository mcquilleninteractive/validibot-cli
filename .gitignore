# Byte-compiled / optimized / DLL files
**/__pycache__/
*.py[cod]
*$py.class

# C extensions
*.so

# Distribution / packaging
.Python
build/
develop-eggs/
dist/
downloads/
eggs/
.eggs/
lib/
lib64/
parts/
sdist/
var/
wheels/
*.egg-info/
.installed.cfg
*.egg

# PyInstaller
*.manifest
*.spec

# Installer logs
pip-log.txt
pip-delete-this-directory.txt

# Unit test / coverage reports
htmlcov/
.tox/
.nox/
.coverage
.coverage.*
.cache
nosetests.xml
coverage.xml
*.cover
*.py,cover
.hypothesis/
.pytest_cache/

# Translations
*.mo
*.pot

# Environments
.env
.venv
env/
venv/
ENV/
env.bak/
venv.bak/

# IDE
.idea/
.vscode/
*.swp
*.swo
*~

# mypy
.mypy_cache/
.dmypy.json
dmypy.json

# ruff
.ruff_cache/

# uv: the lockfile IS committed — it gives reproducible dev/CI installs and lets
# CI catch CVEs in the resolved set. (The published wheel still ships >= floors;
# the lock governs the repo's own environment, not downstream installs.)
# The exported audit requirements file is a generated CI artifact, not source.
requirements-audit.txt

# OS
.DS_Store
Thumbs.db
Desktop.ini

# Security — never commit secrets or key material
*.pem
*.key
*.p12
credentials.json
service-account.json
db.sqlite3

# Local development - symlinks to sibling projects
# (for local development alongside ../validibot)
vb_shared_dev/

# Temporary files
tmp/

# Local config that may contain secrets
.envs/

# AI generated files
.claude

.DS_Store
